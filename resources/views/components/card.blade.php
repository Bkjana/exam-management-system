<div class=" col-sm-6">
    <div class="card">
        <div class="card-header">
          {{$cardName}}
        </div>
        <div class="card-body">
          <h5 class="card-title">{{$cardTitle}}</h5>
          <p class="card-text">{{$cardDesc}}</p>
          <a href="{{$cardLink}}" class="btn btn-primary">{{$cardLinkName}}</a>
        </div>
      </div>
</div>